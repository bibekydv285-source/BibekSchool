@{
    var userName = User.Identity?.Name ?? "Teacher";
    var currentAction = ViewContext.RouteData.Values["action"]?.ToString() ?? "";
}

<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>@ViewData["Title"] - Bibek School Teacher</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-T3c6CoIi6uLrA9TneNEoa7RxnatzjcDSCmG1MXxSR1GAsXEV/Dwwykc2MPK8M2HN" crossorigin="anonymous">
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.2/css/all.min.css" rel="stylesheet">

    <!-- Base site styles first -->
    <link href="~/css/site.css" rel="stylesheet" asp-append-version="true" />
    <link href="~/css/dashboard.css" rel="stylesheet" asp-append-version="true" />
    <link href="~/css/responsive.css" rel="stylesheet" asp-append-version="true" />

    <!-- teacher.css MUST load last so its layout rules win -->
    <link href="~/css/teacher.css" rel="stylesheet" asp-append-version="true" />
</head>
<body class="teacher-theme">
    <a href="#main-content" class="skip-link">Skip to main content</a>

    <div class="app-shell">
        <!-- Sidebar -->
        <aside class="sidebar" id="sidebar" role="navigation" aria-label="Main navigation">
            <div class="sidebar-header">
                <a href="@Url.Action("Dashboard", "Teacher")" class="brand-link">
                    <i class="fas fa-chalkboard-teacher fa-lg me-2"></i>
                    <span class="fw-bold">Bibek School</span>
                    <span class="badge bg-light text-success ms-2 small">Teacher</span>
                </a>
            </div>
            <nav class="sidebar-nav">
                <ul class="nav flex-column">
                    <li class="nav-item">
                        <a class="nav-link @(currentAction == "Dashboard" ? "active" : "")" href="@Url.Action("Dashboard", "Teacher")">
                            <i class="fas fa-tachometer-alt me-2"></i>Dashboard
                        </a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link @(currentAction == "Classes" ? "active" : "")" href="@Url.Action("Classes", "Teacher")">
                            <i class="fas fa-door-open me-2"></i>My Classes
                        </a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link @(currentAction == "Subjects" ? "active" : "")" href="@Url.Action("Subjects", "Teacher")">
                            <i class="fas fa-book me-2"></i>My Subjects
                        </a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link @(currentAction == "Marks" ? "active" : "")" href="@Url.Action("Marks", "Teacher")">
                            <i class="fas fa-clipboard-check me-2"></i>Marks
                        </a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link @(currentAction == "Results" ? "active" : "")" href="@Url.Action("Results", "Teacher")">
                            <i class="fas fa-chart-bar me-2"></i>Results
                        </a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link @(currentAction == "Notifications" ? "active" : "")" href="@Url.Action("Notifications", "Teacher")">
                            <i class="fas fa-bell me-2"></i>Notifications
                            @if (ViewBag.UnreadNotificationsCount > 0)
                            {
                                <span class="badge bg-danger ms-auto">@ViewBag.UnreadNotificationsCount</span>
                            }
                        </a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="@Url.Action("Index", "Profile")">
                            <i class="fas fa-user me-2"></i>Profile
                        </a>
                    </li>
                    <li class="nav-item">
                        <hr class="sidebar-divider" />
                    </li>
                    <li class="nav-item">
                        <form asp-action="Logout" asp-controller="Account" method="post" class="w-100">
                            <button type="submit" class="nav-link w-100 text-danger d-flex align-items-center justify-content-start px-3 py-2" style="background: none; border: none;">
                                <i class="fas fa-sign-out-alt me-2"></i>Logout
                            </button>
                        </form>
                    </li>
                </ul>
            </nav>
        </aside>

        <!-- Sidebar overlay for mobile -->
        <div class="sidebar-overlay" id="sidebarOverlay" aria-hidden="true"></div>

        <!-- Main content area -->
        <div class="main-content">
            <!-- Topbar -->
            <header class="topbar" role="banner">
                <div class="topbar-inner">
                    <button class="btn btn-ghost d-lg-none" id="sidebarToggle" aria-label="Toggle navigation menu" aria-expanded="false" aria-controls="sidebar">
                        <i class="fas fa-bars"></i>
                    </button>
                    <h4 class="topbar-title mb-0">@ViewData["Title"]</h4>
                    <div class="dropdown ms-auto">
                        <a class="btn btn-ghost d-flex align-items-center gap-2" href="#" role="button" data-bs-toggle="dropdown" aria-expanded="false" aria-label="User menu">
                            <i class="fas fa-user-circle fa-lg"></i>
                            <span class="d-none d-md-inline">@userName</span>
                            <span class="badge bg-success ms-2 d-none d-sm-inline">Teacher</span>
                        </a>
                        <ul class="dropdown-menu dropdown-menu-end">
                            <li><a class="dropdown-item" href="@Url.Action("Index", "Profile")"><i class="fas fa-user me-2"></i>Profile</a></li>
                            <li><a class="dropdown-item" href="@Url.Action("ChangePassword", "Profile")"><i class="fas fa-key me-2"></i>Change Password</a></li>
                            <li><hr class="dropdown-divider" /></li>
                            <li>
                                <form asp-action="Logout" asp-controller="Account" method="post">
                                    <button type="submit" class="dropdown-item"><i class="fas fa-sign-out-alt me-2"></i>Logout</button>
                                </form>
                            </li>
                        </ul>
                    </div>
                </div>
            </header>

            <!-- Content -->
            <main class="content" id="main-content" role="main">
                @if (TempData["Success"] != null)
                {
                    <div class="alert alert-success alert-dismissible fade show" role="alert">
                        @TempData["Success"]
                        <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
                    </div>
                }
                @if (TempData["Error"] != null)
                {
                    <div class="alert alert-danger alert-dismissible fade show" role="alert">
                        @TempData["Error"]
                        <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
                    </div>
                }

                @RenderBody()
            </main>
        </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js" integrity="sha384-C6RzsynM9kWDrMNeT87bh95OGNyZPhcTNXj1NW7RuBCsyN/o0jlpcV8Qyq46cDfL" crossorigin="anonymous"></script>
    <script src="~/js/site.js"></script>
    <script src="~/js/teacher.js" asp-append-version="true"></script>
    @RenderSection("Scripts", required: false)
</body>
</html>
